# Ultimate Feature Map

Core planner: hierarchy, reverse linking, deadlines, time blocks, priorities, status, estimates, tags and AI breakdown.

Modules: knowledge, habits, journal, finance, health, learning, travel, contacts, vault, automations, risks, issues and subscriptions.

Views: command dashboard, planner columns, calendar, Kanban, focus mode, vault, module pages, analytics, templates and settings.

Security: Supabase Auth, per-user RLS policies, private storage buckets, local-first fallback and JSON backup.
