# Planned-out

A local-first planner that turns yearly goals into monthly, weekly, and daily actions.

## Run locally

```bash
npm install
npm run dev
```

The app stores plans in the browser's local storage and deploys as a standard Next.js app on Vercel.
